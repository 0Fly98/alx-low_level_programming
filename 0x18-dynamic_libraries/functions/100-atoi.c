#include "main.h"
#include <stdlib.h>

/**
 * _atoi - converts a string to an integer
 * @s: the string to the convert
 *
 * Return: the converted integer value
 */
int _atoi(char *s)
{
	return (atoi(s));
}
