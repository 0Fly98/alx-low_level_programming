#include "main.h"
#include <stdio.h>
#include <ctype.h>

/**
 * _strlen - Calculates the length of a string.
 *
 * @s: The string to be easured.
 *
 * Return: The length of the sttring @s.
 */
int _strlen(char*s)
{
	return strlen(s);
}
